#! /bin/bash

STEP0(){ DEBUG="STEP0 in install/20_COMPUTE-03.conf"
LIST_MACHINE="$LIST_MACHINE COMPUTE-03"
}

# Sets some vars
# this is unecessary if one want to do it manually
STEP1(){ DEBUG="STEP1 in install/20_COMPUTE-03.conf"
if [ "$MACHINE" = "COMPUTE-03" ]; then
 COMP="GCC"
 OPENMP="NO"
 DOWN="YES"
fi
}



# There is nothing specific to do here.
# nothing is installed on this machine
# everyting must be compiled

  STEP$1 2>/dev/null


